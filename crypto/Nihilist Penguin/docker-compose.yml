version: "3.9"

services:
  jwt_site:
    build: .
    container_name: jwt_confusion_challenge
    ports:
      - "8080:8080"
    environment:
      FLAG: "SKYDAYS{lo_siento_wilson}"
      DB_PATH: "/tmp/app.db"
      TOKEN_TTL_MINUTES: "30"
