version: '3'

services:
  websocket-ctf:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - DOCKER=true
      - HOST=0.0.0.0
      - PORT=8080
    volumes:
      - ./frontend:/app/frontend 