# Docker Compose configuration for CTF application
version: '3.8'

services:
  # Production build (nginx + static files)
  app-prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: client-site-enc-prod