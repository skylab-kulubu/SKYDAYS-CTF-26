# docker-compose.override.yml
# Local development overrides - automatically loaded by docker-compose
# WARNING: Only uncomment sections when needed for development
version: '3.8'

services:
  # MySQL - uncomment for direct database access during development
  # mysql:
  #   ports:
  #     - "3306:3306"

  # Backend - uncomment for direct API access during development
  # backend:
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     DEBUG: "true"
  #     LOG_LEVEL: debug
  #     FRONTEND_URL: "http://localhost:3000"
  #     ALLOWED_ORIGINS: '["http://localhost:3000","http://frontend:3000","http://localhost:5173"]'

  # Frontend - uncomment for direct access during development
  # frontend:
  #   ports:
  #     - "3000:80"
  #   environment:
  #     VITE_DEV_MODE: "true"

  # Nginx - development settings
  nginx:
    environment:
      NGINX_HOST: localhost
