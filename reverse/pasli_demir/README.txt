simdilik basit bir reversing sorusu, rust'ta oldugu için bu duzeyde bırakılabilir veya daha da zorlaştırılabilir.

```
cargo build --release ile build ediniz
```

Daha sonra output binary'yı yarışmacıya veriniz: `./target/release/pasli_demir`
